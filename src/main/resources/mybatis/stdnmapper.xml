<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myadmin.mapper.StdnMapper">

    <select id="getpage" resultType="Stdn">
        SELECT s.img, s.id, s.name, a.is_attend as isAttend, s.email, s.contact, a.rdate as attdDate
        FROM stdn s
        LEFT OUTER JOIN attd a on s.id = a.stdn_id
        WHERE (a.rdate IS NULL OR a.rdate = TO_CHAR(CURRENT_DATE, 'YYYY.MM.DD'))
    </select>
    <select id="searchpage" resultType="Stdn" parameterType="StdnSearch">
        SELECT *
        FROM stdn
        WHERE 1=1
        <if test="search1 != ''">
            AND id LIKE '%' ||  #{search1} || '%'
        </if>
        <if test="search2 != ''">
            AND name LIKE '%' ||  #{search2} || '%'
        </if>
    </select>
    <select id="select" parameterType="String" resultType="Stdn">
        SELECT *
        FROM stdn
        WHERE id=#{id}
    </select>
    <select id="selectall" resultType="Stdn">
        SELECT *
        FROM stdn
    </select>
    <insert id="insert" parameterType="Stdn">
        INSERT INTO stdn (id, pwd, name, email, contact)
        VALUES (#{id}, #{pwd}, #{name}, #{email}, #{contact})
    </insert>
    <update id="update" parameterType="Stdn">
        UPDATE stdn
        SET pwd=#{pwd}, name=#{name}, email=#{email}, contact=#{contact}
        WHERE id=#{id}
    </update>
    <delete id="delete" parameterType="String">
        DELETE
        FROM stdn
        WHERE id=#{id}
    </delete>
</mapper>